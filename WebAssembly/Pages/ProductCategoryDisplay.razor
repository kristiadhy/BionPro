@page "/products/categories"

<PageTitle>@($"{GlobalState.AppName} | {ProductCategoryPageModel?.Title}")</PageTitle>

@* <ProductSubButtons PageModel="ProductCategoryPageModel" /> *@
<ProductSubMenu />

<MainMenuContainer>
    <RadzenText TextStyle="TextStyle.H5">@ProductCategoryPageModel?.Title</RadzenText>
    <BreadCrumb breadCrumbs="@BreadCrumbs" />
</MainMenuContainer>

<MainMenuContainer>
    <MainMenuButton LoadDataClicked="EvReloadData"
                    CreateNewClicked="EvCreateNew" />
</MainMenuContainer>

<RadzenDataGrid @ref="ProductCategoryGrid"
                IsLoading=@isLoading
                Data="@ProductCategoryState.ProductCategoryList"
                SelectionMode="DataGridSelectionMode.Single"
                AllowSorting="true"
                AllowFiltering="false"
                AllowColumnResize="true"
                AllowColumnReorder="false"
                GridLines="DataGridGridLines.Both"
                ColumnWidth="150px"
                class="my-4">
    <Columns>
        <RadzenDataGridColumn Width="50px"
                              Title="#" Filterable="false"
                              Sortable="false"
                              TextAlign="TextAlign.Center">
            <Template Context="data">
                @(ProductCategoryState.ProductCategoryList.IndexOf(data) + 1)
            </Template>
        </RadzenDataGridColumn>

        <RadzenDataGridColumn Property="CategoryID"
                              Visible=false
                              Filterable="false"
                              Title="ID"
                              TextAlign="TextAlign.Center">
        </RadzenDataGridColumn>

        <RadzenDataGridColumn Property="Name"
                              Title="Name"
                              Frozen="true" />

        <RadzenDataGridColumn Context="ProductCategoryAction"
                              Title="Action"
                              Filterable="false"
                              Sortable="false"
                              TextAlign="TextAlign.Left"
                              Frozen="true"
                              FrozenPosition="FrozenColumnPosition.Right">
            <Template Context="ProductCategoryDto">
                <RadzenButton Icon="edit"
                              ButtonStyle="ButtonStyle.Light"
                              Variant="Variant.Flat"
                              Size="ButtonSize.Medium"
                              Click="@(args => EvEditRow(ProductCategoryDto))"
                              @onclick:stopPropagation="true">
                </RadzenButton>
                <RadzenButton ButtonStyle="ButtonStyle.Danger"
                              Icon="delete"
                              Variant="Variant.Flat"
                              Shade="Shade.Lighter"
                              Size="ButtonSize.Medium"
                              class="my-1 ms-1"
                              Click="@(async args => await EvDeleteRow(ProductCategoryDto))"
                              @onclick:stopPropagation="true">
                </RadzenButton>
            </Template>
        </RadzenDataGridColumn>
    </Columns>
</RadzenDataGrid>

<Pager PageSize="@ProductCategoryState.MetaData.PageSize"
       TotalCount="@ProductCategoryState.MetaData.TotalCount"
       PageChanged="@(args => PageChanged(args))" />